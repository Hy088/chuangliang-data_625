.tmp_check/
_exports/
.workbuddy/
.env

# 本地测试日志
_serve_*.log

# 超大素材明细：线上走 .gz（前端自动解压），原始 JSON 留在本地
period-materials.json

# 同步脚本的备份产物 / 按周期明文分片（线上只托管 .gz）
*.syncbak
period-materials-*.json
_bak_period_*/
_trash/
__pycache__/

# 即梦浏览器自动化：Cookie 与下载产物（含账号态，勿提交）
jimeng-cookie.json
jimeng-output/

# 周期报表自动导出：创量账号凭据（含密码，绝不提交）
period-export.config.json
